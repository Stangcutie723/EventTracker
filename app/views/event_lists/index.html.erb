<h1>Listing of Important Events!</h1>
<h2>Try not to forget these ones...</h2>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Date</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @event_lists.each do |event_list| %>
      <tr>
        <td><%= event_list.title %></td>
        <td><%= event_list.date %></td>
        <td><%= link_to 'Show', event_list %></td>
        <td><%= link_to 'Edit', edit_event_list_path(event_list) %></td>
        <td><%= link_to 'Destroy', event_list, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Add New Event', new_event_list_path %>
<h3>That is a lot to remember...Damn!</h3>
<h3> Good thing this is here!</h3>
